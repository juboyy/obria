"use client";
import { useEffect, useMemo, useState } from "react";

type Demo = any;
const money = (cents: number) => new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(cents / 100);
const quantity = (milli: number, unit: string) => `${(milli / 1000).toLocaleString("pt-BR", { maximumFractionDigits: 2 })} ${unit}`;

export default function DemoPage() {
  const [role, setRole] = useState<"CLIENT" | "SUPPLIER">("CLIENT");
  const [data, setData] = useState<Demo | null>(null);
  const [busy, setBusy] = useState("");
  const [message, setMessage] = useState("Demo determinística com resultados previamente gerados por APIs reais.");
  async function load(nextRole = role) { const response = await fetch("/api/demo", { headers: { "x-obria-role": nextRole }, cache: "no-store" }); if (response.ok) setData(await response.json()); }
  async function action(path: string, body?: unknown, nextRole = role) { setBusy(path); setMessage("Processando no domínio Obria…"); const response = await fetch(path, { method: "POST", headers: { "content-type": "application/json", "x-obria-role": nextRole }, body: body === undefined ? undefined : JSON.stringify(body) }); const result = await response.json(); setBusy(""); if (!response.ok) { setMessage(result.error?.message ?? "A ação não pôde ser concluída."); return; } setMessage("Atualizado. O estado foi persistido e pode ser conferido após refresh."); await load(nextRole); }
  useEffect(() => { void load(); }, [role]);
  const latest = useMemo(() => data?.versions?.at(-1), [data]);
  const approved = data?.versions?.find((version: Demo) => version.state === "APPROVED");
  const phase = data?.project?.state ?? "DRAFT";
  const phaseIndex = ["DESIGN_READY", "PLAN_APPROVED", "OPEN_FOR_QUOTES", "AWAITING_ACCEPTANCE", "ACCEPTED"].indexOf(phase);
  if (!data) return <main className="app-shell"><header className="topbar"><div className="brand"><span className="brand-mark">O</span> obria</div><span className="mono">carregando sala…</span></header></main>;
  return <main className="app-shell">
    <header className="topbar"><div className="brand"><span className="brand-mark">O</span> obria</div><div className="status"><span className="status-dot" /> Storage · dados · MCP <strong>OK</strong></div></header>
    <section className="hero"><div><div className="eyebrow mono">prancheta de obra contemporânea / 001</div><h1>Do primeiro clique à sala pronta.</h1><p>Uma foto vira decisão mensurável: versão visual, quantitativos, referência de custo, impacto e uma proposta que chega com contexto.</p><div className="actions"><a className="btn" href="#workspace">Abrir sala demo</a><a className="btn secondary" href="#proof">Ver o que é real</a></div></div><aside className="hero-note"><strong>Uma sala. Dois papéis. Zero botão cenográfico.</strong><span className="muted">O cliente mantém a autoria do plano. O fornecedor recebe somente o briefing publicável.</span></aside></section>
    <section className="workspace" id="workspace"><div className="role-switch" aria-label="Selecionar papel"><button className={role === "CLIENT" ? "active" : ""} onClick={() => setRole("CLIENT")}>Cliente</button><button className={role === "SUPPLIER" ? "active" : ""} onClick={() => setRole("SUPPLIER")}>Fornecedor</button></div>
      {role === "CLIENT" ? <ClientView data={data} phaseIndex={phaseIndex} latest={latest} approved={approved} busy={busy} message={message} action={action} /> : <SupplierView data={data} busy={busy} message={message} action={action} />}
    </section>
    <footer className="footer" id="proof"><span className="mono">OBRIA / MVP HACKATHON</span><span>Replay fixo: São Paulo · 4,0 × 3,0 × 2,7 m · relógio 19 ago 2026</span></footer>
  </main>;
}

function ClientView({ data, phaseIndex, latest, approved, busy, message, action }: Demo) {
  const project = data.project; const canIterate = phaseIndex < 1 || phaseIndex === -1; const canApprove = latest?.state === "READY" && project.state === "DESIGN_READY"; const canPublish = project.state === "PLAN_APPROVED"; const canAccept = project.state === "AWAITING_ACCEPTANCE" && data.proposals?.length;
  return <><div className="steps">{["Briefing", "Iterações", "Aprovação", "Marketplace", "Aceite"].map((label: string, index: number) => <div className={`step ${index < phaseIndex + 1 ? "done" : ""} ${index === phaseIndex + 1 ? "current" : ""}`} key={label}>{String(index + 1).padStart(2, "0")} · {label}</div>)}</div><div className="grid"><div className="panel"><div className="room-image"><img src={approved?.imageDataUri ?? latest?.imageDataUri} alt="Visualização da sala demo" /><span className="image-tag mono">{approved ? "versão aprovada" : `versão ${latest?.number}`}</span></div><div style={{ display: "flex", justifyContent: "space-between", gap: 14, marginTop: 18 }}><div><div className="mono muted">projeto</div><h2>{project.title}</h2></div><div className="mono">{project.state}</div></div><p>{latest?.summary}</p><div className="chips">{latest?.plan?.interventions?.map((item: Demo) => <span className="chip" key={item.catalogKey}>{item.catalogKey.replaceAll("_", " ")}</span>)}</div><div className="actions"><button className="btn" disabled={!canIterate || !!busy} onClick={() => action(`/api/projects/${project.id}/designs`, { prompt: "menos marcenaria, mais plantas" })}>{busy.includes("designs") ? "Gerando…" : "Iterar com MCP"}</button><button className="btn secondary" disabled={!canApprove || !!busy} onClick={() => action(`/api/projects/${project.id}/approve`, { versionId: latest.id, expectedRevision: project.revision })}>Aprovar versão {latest?.number}</button><button className="btn secondary" disabled={!canPublish || !!busy} onClick={() => action(`/api/projects/${project.id}/publish`)}>Publicar para cotação</button>{approved && <button className="btn secondary" disabled={!!busy} onClick={() => action(`/api/projects/${project.id}/world`)}>Abrir 3D real</button>}{canAccept && <button className="btn" disabled={!!busy} onClick={() => action(`/api/proposals/${data.proposals[0].id}/accept`)}>Aceitar proposta</button>}</div><div className={`notice ${project.state === "ACCEPTED" ? "success" : ""}`} role="status">{message}</div></div><aside className="panel dark"><div className="mono">briefing mensurável</div><h2>O que não se perde no caminho.</h2><div className="meta-row"><span className="meta-label">Dimensões</span><strong>{(project.brief.lengthMm / 1000).toFixed(1).replace(".", ",")} × {(project.brief.widthMm / 1000).toFixed(1).replace(".", ",")} × {(project.brief.heightMm / 1000).toFixed(1).replace(".", ",")} m</strong></div><div className="meta-row"><span className="meta-label">Cidade</span><strong>{project.brief.city} / {project.brief.state}</strong></div><div className="meta-row"><span className="meta-label">Orçamento</span><strong>{money(project.brief.budgetCents)}</strong></div><div className="meta-row"><span className="meta-label">Preservar</span><strong>{project.brief.preserve.join(", ")}</strong></div><div style={{ marginTop: 28 }}><div className="mono">análise com incerteza</div>{project.analysis.observed.map((item: string) => <p className="small" key={item}>+ {item}</p>)}<p className="small" style={{ color: "var(--coral)" }}>? {project.analysis.uncertainties[0]}</p></div></aside></div><div className="grid" style={{ marginTop: 20 }}><div className="panel"><div className="mono muted">histórico imutável</div><h2>Versões que podem ser cotadas.</h2><div className="version-list">{data.versions.map((version: Demo) => <div className={`version ${version.state === "APPROVED" ? "approved" : ""}`} key={version.id}><div><strong>V{version.number} · {version.state}</strong><div className="small muted">{version.summary}</div></div><span className="cost">{money(version.costCents)}</span></div>)}</div></div><div className="panel"><div className="mono muted">referências oficiais fixas</div><h2>Decisão com lastro.</h2><p>Quantidades são recalculadas em milímetros. Valores são centavos. A referência pública é SINAPI/SP · 2026-07, não cotação em tempo real.</p>{(approved ?? latest).quantities.slice(0, 5).map((line: Demo) => <div className="quote-line" key={line.catalogKey}><span>{line.referenceLabel}<br /><small className="muted">{quantity(line.quantityMilli, line.unit)} · {line.referenceCode}</small></span><strong>{money(line.totalCents)}</strong></div>)}<div className="quote-total"><span>Total de referência</span><strong>{money((approved ?? latest).costCents)}</strong></div></div></div></>;
}

function SupplierView({ data, busy, message, action }: Demo) {
  const opportunity = data.opportunity; const proposal = data.proposals?.[0]; const lines = opportunity?.quantities?.map((item: Demo) => ({ catalogKey: item.catalogKey, quantityMilli: item.quantityMilli, priceCents: Math.round(item.totalCents * 1.08), note: "fornecimento e instalação" })) ?? [];
  return <div className="grid"><div className="panel dark"><div className="mono">folha técnica / oportunidade</div><h2>{opportunity ? opportunity.title : "A demanda ainda está fechada."}</h2>{opportunity ? <><div className="room-image"><img src={opportunity.imageDataUri} alt="Imagem aprovada compartilhada com fornecedor" /><span className="image-tag mono">briefing aprovado</span></div><div className="meta-row"><span className="meta-label">Local</span><strong>{opportunity.city} / {opportunity.state}</strong></div><div className="meta-row"><span className="meta-label">Prazo de resposta</span><strong>7 dias</strong></div><div className="meta-row"><span className="meta-label">Impacto estimado</span><strong>{(opportunity.co2Grams / 1000).toLocaleString("pt-BR")} kg CO₂</strong></div><div className="actions"><button className="btn" disabled={!!proposal || !!busy} onClick={() => action(`/api/opportunities/${opportunity.id}/proposals`, { lines, freightCents: 18000, taxCents: 72000, leadDays: 18, validUntil: "2026-09-03T12:00:00.000Z", includes: ["montagem", "proteção do piso"], excludes: ["obra estrutural"] }, "SUPPLIER")}>{proposal ? "Proposta enviada" : "Revisar e enviar proposta"}</button></div></> : <p>O cliente precisa aprovar e publicar o plano antes de a demanda aparecer aqui.</p>}</div><aside className="panel"><div className="mono muted">proposta do fornecedor</div><h2>{proposal ? proposal.state : "sem rascunho"}</h2>{proposal ? <><div className="quote-total"><span>Preço de referência</span><strong>{money(opportunity.costCents)}</strong></div><div className="quote-total"><span>Total proposto</span><strong>{money(proposal.totalCents)}</strong></div><div className="meta-row"><span className="meta-label">Entrega</span><strong>{proposal.leadDays} dias</strong></div><p className="small">Inclui: {proposal.includes.join(", ")}</p><p className="small">Exclui: {proposal.excludes.join(", ")}</p></> : <p>Os valores só aparecem depois do preenchimento das linhas, frete, impostos e prazo.</p>}<div className={`notice ${proposal?.state === "ACCEPTED" ? "success" : ""}`} role="status">{message}</div></aside></div>;
}
